title: Cadre Documentation
description: AI co-reviewer for parametric CAD — model parts in the browser and catch manufacturability issues grounded in measured geometry.
url: https://docs.cadre3d.com
baseurl: ""
lang: en
author: Cadre

# GitHub Pages default-safe plugins (explicit so SEO tags + sitemap always emit)
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-relative-links
  - jekyll-default-layout
  - jekyll-optional-front-matter
  - jekyll-titles-from-headings

exclude:
  - README.md
